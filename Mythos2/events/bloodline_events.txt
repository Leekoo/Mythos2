namespace = MBL

# Add a test bloodline
character_event = {
	id = MBL.1
	desc = TEST
	
	is_triggered_only = yes
	
	option = {
		text = TEST_RESPONSE
		create_bloodline = {
			type = test_bloodline
		}
	}
}

# Give test bloodline to spouse(s)
character_event = {
	id = MBL.2
	desc = TEST2
	
	is_trigged_only = yes
	
	option = {
		text = TEST_RESPONSE
		any_bloodline = {
			limit = { has_bloodline_flag = test }
			ROOT = {
				any_spouse = {
					add_to_bloodline = PREVPREV
				}
			}
		}
	}
}
	